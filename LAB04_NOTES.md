# LAB04 — التصحيحات ومعاملات Delta والصيانة

أمل خوتاني — Amal Khotani. برنامج **هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)**، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy

مواد الدورة ودوالها: **ميعاد المري — Meaad Al-Marri**. هذه ملاحظات مبنية على المخرجات المحفوظة في الدفتر المرفوع والتقارير الأصلية؛ لم يُعد تشغيل Spark أثناء إعدادها. تفاصيل البيئة وبصمات الأدلة وحدود التحقق في [EVIDENCE_INDEX.md](EVIDENCE_INDEX.md).

## التعريف وما نُفذ

اليوم الثالث في [day03/STUDENT.ipynb](day03/STUDENT.ipynb)، الخلايا الأصلية 59–65. التشغيل `673e47762ceb46a395a30edc88d52e2a` للمعاملات، و`49d000d16d594d5aab996182e96956bd` للصيانة. تحفظ تواريخ سجل المعاملات 16 سبتمبر 2026. نجحت فحوص المعاملات الستة وفحوص الصيانة السبعة.

الدليل: `reports/day03_transactions.json` و`reports/day03_maintenance_latest.json` داخل WORK. نسخ الصيانة معزولة تحت `sandbox/day03/`؛ الجدول المستمر هو `mini_lakehouse/silver/trips/`.

## إجابات أسئلة اليوم الثالث الثمانية

1. **نوعا النسخة:** `source_revision` أولوية مراجعة سجل المصدر، وDelta version رقم معاملة الجدول. قبل التصحيح كانت نسخة الجدول 1، وبعده 2؛ كون مراجعة المصدر الجديدة 2 مطابقًا لنسخة الجدول 2 هنا مصادفة عددية، وليس تعريفًا واحدًا. ارتفعت نسخة الجدول لاحقًا إلى 3 و4 دون تغيير قيم الرحلة المصححة.
2. **مطابقة التصحيح:** تغيرت `SYN_T0001` من 18.00 إلى 23.00 ريالًا ومراجعة المصدر إلى 2. بقيت 75 رحلة لأن الصف عُدّل بالمفتاح نفسه. الزيادة 5.00 ريال تفسر الانتقال من 1875.60 إلى 1880.60.
3. **القديم والتعارض:** وصول قيمة 18.00 بمراجعة أقدم لا يستبدل قيمة 23.00. أما 24.00 بالمراجعة 2 نفسها فيمثل تضاربًا ويرفض. فحصا `replay_and_stale_delivery_preserve_values` و`same_revision_conflict_rejected` ناجحان؛ تطابقت بصمات الأعمال بعد التصحيح والإعادتين.
4. **الرفض المتوقع:** غياب مكتبة لا يثبت قيد البيانات. تجربة الكتابة المختلطة احتوت صفًا صالحًا وآخر مخالفًا لقيد الأجرة؛ وثّق التقرير رفضًا من المحرك من النوع `Py4JJavaError` في اختبار `constraint`، وبقاء عدد الصفوف 75 والنسخة 2 والبصمة نفسها قبل وبعد المحاولة. لم يُعتمد أي من الصفين.
5. **نسخة المخطط تبدأ بـ74:** تُستثنى `SYN_T0002` أولًا، ثم يضاف صفها الموسع، فيعود العدد إلى 75. إضافتها إلى نسخة كاملة من 75 تنتج 76 صفًا وتكرر رحلة الأعمال. تغيير المخطط غير المعتمد رُفض، ثم نجح التطوير المقصود في النسخة المعزولة.
6. **حقل surcharge_sar:** القيمة 2.00 موجودة لصف واحد، والقيم الفارغة 74. لا تغير `fare_sar` ولا إجمالي الأجور تلقائيًا؛ هي إضافة مخطط تجريبية تحتاج تعريفًا تجاريًا وموافقة قبل تضمينها في التقرير. لم تُعمم على Silver الأساسية.
7. **تاريخ RESTORE:** نسخة الاستعادة بدأت بـversion 0 و75 صفًا؛ أصبح العدد 74 في version 1 بعد DELETE، ثم 75 في version 2 بعد RESTORE. لا يرجع رقم النسخة إلى الخلف؛ تُنشأ معاملة جديدة تعيد المحتوى السابق. في المقابل، قراءة نسخة قديمة بـversionAsOf لا تغير الجدول. قرأ تمرين المعاملات النسخة السابقة 1 فعلًا.
8. **VACUUM DRY RUN:** `dry_run_only=true`، والاحتفاظ 168 ساعة، وعدد المرشحين 0، و`files_deleted_by_vacuum=0`. خلو القائمة نتيجة صحيحة ممكنة، وليس دليل حذف أو فشل. لم تُخفض مدة الاحتفاظ لإجبار ظهور ملفات.

## الصيانة والقرار

انخفضت الملفات النشطة في نسخة compaction من 4 إلى 1، وحجمها من 28486 إلى 12739 بايت، مع ثبات قيم الأعمال. هذا لا يعني حذف كل الملفات التاريخية ولا يثبت تسارع القراءة؛ `speedup_claimed=false`. التفاصيل في [BENCHMARKS.md](BENCHMARKS.md).

تُجرى تجارب الحذف والمخطط والاستعادة على نسخ معزولة لحماية المسار التراكمي. أكد `trusted_silver_unchanged` ثبات Silver الأساسية. لا تثبت هذه التجربة استعادة كارثة متعددة الآلات أو معاملة واحدة عبر جداول متعددة. يبدأ اليوم الرابع من Silver المصححة، ولا تعاد تحويلات اليوم الثاني عليها.
