# LAB03 — Staging وSilver والتحويل باستخدام dbt

أمل خوتاني — Amal Khotani. برنامج **هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)**، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy

مواد الدورة ودوالها: **ميعاد المري — Meaad Al-Marri**. هذه ملاحظات مبنية على المخرجات المحفوظة في الدفتر المرفوع والتقارير الأصلية؛ لم يُعد تشغيل Spark أثناء إعدادها. تفاصيل البيئة وبصمات الأدلة وحدود التحقق في [EVIDENCE_INDEX.md](EVIDENCE_INDEX.md).

## التعريف وما نُفذ

اليوم الثاني في [day02/STUDENT.ipynb](day02/STUDENT.ipynb)، الخلايا الأصلية 48–56. نُفّذت التهيئة ثم Silver التزايدية، وبعدها مسار dbt مستقل. الاستمرار يستخدم مساحة Bronze نفسها، لا بيانات جديدة بديلة.

## الأدلة والنتائج

التهيئة تحتفظ بـ144 نسخة استقبال للرحلات و6 سائقين و216 حدثًا، ونجحت فحوصها الخمسة. جرى تحويل الأنواع وتوحيد المدن والوقت، مع التحقق من تفرد السائق قبل الربط. مثال المصدر ` riyadh ` يصبح `Riyadh`، وبداية `SYN_T0001` عند `06:00+03:00` تمثل `03:00Z`.

| السيناريو | نسخ Bronze | رحلات Silver | Delta version |
|---|---|---|---|
| base_and_replay | 144 | 72 | 0 |
| same_input_rerun | 144 | 72 | 0 |
| late_batch | 147 | 75 | 1 |
| same_batch_retry | 147 | 75 | 1 |
| intentional_redelivery | 150 | 75 | 1 |

بلغ مجموع الأجور بعد التأخر 1875.60 ريالًا، ونجحت فحوص Silver الخمسة. مسار الجدول المستمر `mini_lakehouse/silver/trips/`. التقارير: `reports/day02_staging_latest.json` و`reports/day02_silver.json` و`reports/day02_expected_vs_actual.json`.

### دليل dbt بعد إعادة التشغيل

بعد فقد ملفات المحاولة السابقة، أُعيد تشغيل `run_dbt_lab(ROOT)` فعليًا في Colab على مساحة معزولة من لقطات Bronze الأصلية. يسجل [DBT_RECOVERY.ipynb](day02/DBT_RECOVERY.ipynb) الاستعادة وإعادة التشغيل وحفظ الأدلة. بدأت المحاولة في `2026-09-16T21:38:35.917764+00:00` وانتهت في `2026-09-16T21:43:23.986216+00:00`.

معرف المحاولة: `83b24dcbe27141009fec1b6eae58baef`. التقرير: `outputs/dbt_validation_tedk3wzl/reports/dbt_attempt.json`. الحالة `PASSED_DBT_NATIVE`، و`dbt_executed=true` و`engine_executed=true`. تقرير فحص الاعتماديات المضمن يسبق التنفيذ؛ لا يُفسر حقله dbt_executed=false على أنه فشل التشغيل اللاحق.

| المرحلة | الرحلات | الأجور بالريال | Delta version في مساحة dbt |
|---|---:|---:|---:|
| base | 72 | 1794.60 | 3 |
| rerun | 72 | 1794.60 | 6 |
| late | 75 | 1875.60 | 10 |
| late_replay | 75 | 1875.60 | 13 |

تطابقت صفوف الأعمال كاملةً بين base وrerun، وبين late وlate_replay. تغير إصدار Delta مع بقاء محتوى الأعمال متساويًا عند الإعادة. هذه الإصدارات تخص مساحة dbt الجديدة، وليست إصدارات جدول Spark المستمر في الجدول السابق.

حُفظت 16 هوية استدعاء مختلفة للمراحل: staging وgate وfreshness وbuild لكل سيناريو. نجحت في كل build النماذج الستة والاختبارات الـ23، ونجحت فحوص freshness للمصادر الثلاثة دون تحذيرات. ولد الأمر السابع عشر توثيق 6 نماذج و3 مصادر. ملفات `index.html` و`manifest.json` و`catalog.json` موجودة معًا في `dbt/commands/documentation/target/` تحت مساحة dbt.

الأرشيف `dbt_evidence_83b24dcb.zip` يحفظ التقرير والتوثيق والنتائج الفعلية. طوبقت 60 بصمة مختلفة لملفات التقرير والأوامر واللقطات وآثار Delta، وطابقت بصمة تقرير Bronze المدخل التقرير الموجود في `day05_handoff.zip`. هذا فحص للأدلة المحفوظة، وليس إعادة تنفيذ مستقلة في بيئة المراجعة.

تظل المحاولة القديمة `outputs/dbt_validation_rbj8s_xq/` ظاهرة في مخرجات اليوم الثاني التاريخية؛ لا ننسب الملفات الجديدة إليها. إعادة dbt المنفصلة تستخدم نتائج اليوم الثاني قبل تصحيح اليوم الثالث؛ لذلك يبلغ مجموعها 1875.60 ريالًا، بينما يبلغ إجمالي Gold بعد التصحيح 1880.60 ريالًا.

## إجابات أسئلة اليوم الثاني الثمانية

1. **144 نسخة وليست 144 رحلة:** مفتاح الأعمال `trip_id`؛ الملف الأساسي ذو 72 رحلة وصل مرتين. تحفظ التهيئة نسخ الوصول وتختار Silver المحتوى الصحيح لكل مفتاح.
2. **تكرار السائق:** يضاعف الربط صف الرحلة إذا طابق أكثر من صف سائق. يجب فحص تفرد `driver_id` قبل الربط والتحقق من ثبات عدد الرحلات بعده؛ فحص `drivers_unique_and_join_safe` ناجح.
3. **رحلة 1 يونيو تصل 4 يونيو:** يستخدم تقرير الطلب تاريخ وقوع الرحلة في المنطقة الزمنية المحددة، وفحص حداثة الوصول يستخدم وقت الوصول. لا تغيّر الساعة الأصلية لتجاوز تحذير الحداثة.
4. **الأجرة المختلفة ليست replay عاديًا:** إنها تغير في محتوى مفتاح قائم، يحتاج سياسة مراجعات وتعارض صريحة. يبقى `correction.csv` لليوم الثالث؛ لا يضاف خفيةً إلى هذا اليوم.
5. **نسخ مختلفة ومحتوى متساوٍ:** نسخة Delta تصف معاملة تخزين، بينما تطابق المحتوى يقارن قيم الأعمال. في هذه الجولات تحديدًا بقيت النسخة 0 عند إعادة الإدخال نفسه، والنسخة 1 عند إعادة المتأخر؛ اختلاف رقم النسخة ليس شرطًا للإعادة الصحيحة.
6. **ربط GPS الخام:** لكل رحلة أساسية ثلاثة أحداث، فيتكرر مبلغ الرحلة ثلاث مرات. الحل تجميع الأحداث عند `trip_id` ثم الربط مع رحلة فريدة، والتحقق من مجموع الأجور؛ 1794.60 × 3 = 5383.80 يمثل التضخم الخاطئ للقاعدة الأساسية.
7. **الحدث الذي قد يفقد:** حدث بتاريخ قديم يصل بعد معالجة تاريخ أحدث، مثل وصول رحلة من 1 يونيو في 4 يونيو. نافذة lookback تعيد فحص فترة ماضية، ثم يعتمد الدمج على مفتاح الأعمال. في dbt تعرف `source()` المصادر، وتعرف `ref()` اعتماد نموذج على نموذج؛ يضبط lookback اختيار الإدخالات المتأخرة ضمن ذلك المسار. لا تُدعى تغطية كل التأخيرات خارج نافذة محدودة.
8. **الخصائص غير المتاحة عند بداية الرحلة:** وقت النهاية والمدة الفعلية والأجرة النهائية والمسافة المكتملة والأحداث التي تصل مستقبلًا؛ استخدامها آنذاك تسرب معلومات. لا يجوز استخدامها إلا إذا كانت متاحة بالفعل عند وقت القرار وبالتعريف المناسب للمهمة.

## القرار والخطوة التالية

تستمر Silver بــ75 رحلة إلى اليوم الثالث. تُحفظ بصمات المحتوى لإثبات أن إعادة التسليم لم تضاعف الرحلات. لم تختبر هذه العينة كُتّابًا متزامنين أو تأخرًا غير محدود. اكتمل حفظ دليل dbt المنفصل للمحاولة الجديدة؛ يبقى ربط أرشيفه وأرشيف اليوم الخامس في التسليم ومراجعة الروابط بعد الرفع.
