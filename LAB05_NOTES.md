# LAB05 — استقبال أحداث Kafka وتتبع الإعادة

أمل خوتاني — Amal Khotani. برنامج **هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)**، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy

مواد الدورة ودوالها: **ميعاد المري — Meaad Al-Marri**. هذه ملاحظات مبنية على المخرجات المحفوظة في الدفتر المرفوع والتقارير الأصلية؛ لم يُعد تشغيل Spark أثناء إعدادها. تفاصيل البيئة وبصمات الأدلة وحدود التحقق في [EVIDENCE_INDEX.md](EVIDENCE_INDEX.md).

## التعريف وما نُفذ

اليوم الرابع في [day04/STUDENT.ipynb](day04/STUDENT.ipynb)، الخلايا الأصلية 69 و71 و72. أُعد Kafka محليًا، ثم شُغّل `scripts/run_day04.py --part streaming` في عملية Python/Spark جديدة لتحميل الموصل. ظهر `KAFKA_READY` ثم `LAB05_PASSED`. معرف التشغيل `868afc51dbd94951819806c340bba5d3`، والموضوع `masar-day04-868afc51dbd94951819806c340bba5d3`.

تعذر المسار السابق لغياب عميل Kafka أو المنفذ، ثم ظهر خطأ غياب موصل Spark رغم جاهزية Kafka. الحل المستخدم فصل تشغيل Spark الجديد عن JVM القديم؛ جاهزية الوسيط وحدها لا تثبت وجود موصل Spark. لا تُنسب الأخطاء البيئية إلى نجاح اختبار أعمال.

## النتائج والأدلة

| المرحلة | صفوف الوصول | أحداث فريدة | هوية التنفيذ |
|---|---|---|---|
| base | 216 | 216 | `8356e63f-89ee-47a7-86c9-2e06ecfd4607` |
| restart | 216 | 216 | `7a10a336-9f46-44bf-9cf2-7f616482232e` |
| replay | 218 | 216 | `b9ccfb2d-7d0f-4288-b140-a13aeec21b84` |
| late | 219 | 217 | `e45f2363-f2f1-47c6-bf5f-d7effba47cad` |

نجحت الفحوص الثلاثة عشر. هوية الاستعلام الثابتة `e1cbee23-e135-411c-99b3-48d584f165cf`، ونقطة التحقق `checkpoints/day04/gps_868afc51dbd94951819806c340bba5d3`. التقرير `reports/day04_stream_latest.json`، واللقطة المعتمدة `mini_lakehouse/silver/gps_events_day04/868afc51dbd94951819806c340bba5d3`؛ مساراتها نسبية إلى WORK. تطابقت مفاتيح إزاحات المنتج والمستهلك لجميع سجلات الوصول الـ219.

## إجابات أسئلة اليوم الرابع: 1–4

1. **الاستئناف:** بقي العدد 216 لأن مرحلة restart لم ترسل أحداثًا جديدة وأعادت استخدام نقطة التحقق نفسها. بقي `query_id` وتغير `query_run_id`. إعادة خلية اللاب كاملة تنشئ run_id وموضوعًا ونقطة جديدة؛ ليست استئنافًا تلقائيًا للتجربة السابقة.
2. **مفتاحان وعددان:** مفتاح الوصول `(topic, partition, offset)`، ومفتاح الأعمال `event_id`. أرسلت replay رسالتين جديدتين بالمحتوى السابق، فصار النقل 218 وبقيت الأحداث الفريدة 216. حماية إعادة محاولة المنتج لا تمنع إرسالًا جديدًا مقصودًا بمعرف أعمال سبق إرساله.
3. **الحدث المتأخر:** `SYN_E_LATE001` له معرف جديد وتوقيت قديم؛ الاحتفاظ به رفع الأحداث الفريدة إلى 217. تثبت الفحوص حفظ هذا المثال ومطابقة محتواه، لكنها لا تثبت سلوك watermark؛ يذكر التقرير صراحة أن `watermark execution` غير مثبت.
4. **التعارض قبل إزالة التكرار:** قد يخفي dropDuplicates اختيارًا اعتباطيًا بين موقعين للمعرف نفسه. داخل `consume_available` يفحص المسار محتوى الأحداث بواسطة `unique_events` قبل إخراج عددها وبصمتها؛ ثم ينفذ `run_stream_lab` إزالة التكرار وتكوين لقطة Delta النهائية. يجب رفض محتوى متعارض للمفتاح نفسه، لا إخفاء أحد السجلين.

## الحفظ والحدود

الأحداث الـ217 مرتبطة برحلات Silver المعتمدة. لا تستخدم Gold التدفق الخام ذي الـ219 نسخة وصول. يحفظ ZIP جداول Delta وتقارير المنتج والمستهلك ونقطة Spark، لكنه لا يحفظ تخزين وسيط Kafka في `/content/masar_kafka/data` الموجود خارج WORK. لذلك لا تكفي استعادة ZIP لإحياء الموضوع القديم على وسيط جديد. لم تختبر هذه الجولة broker failover أو ضغط استعادة التعطل أو نشرًا موزعًا. قرارات الوصول والاحتفاظ في [GOVERNANCE.md](GOVERNANCE.md).
