# LAB02 — افتراضات التكلفة والقياس الفعلي

أمل خوتاني — Amal Khotani. برنامج **هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)**، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy

مواد الدورة ودوالها: **ميعاد المري — Meaad Al-Marri**. هذه ملاحظات مبنية على المخرجات المحفوظة في الدفتر المرفوع والتقارير الأصلية؛ لم يُعد تشغيل Spark أثناء إعدادها. تفاصيل البيئة وبصمات الأدلة وحدود التحقق في [EVIDENCE_INDEX.md](EVIDENCE_INDEX.md).

## التعريف وما نُفذ

اليوم الأول، جزء التكلفة ثم قياس Spark في [day01/STUDENT.ipynb](day01/STUDENT.ipynb)، خلايا المصدر 26–42. شُغّل نموذج تكلفة تعليمي وفحوص حدود، ثم مقارنة الاستعلام نفسه على CSV وDelta version 0 مع قياسات فعلية. التفاصيل الكمية والخطط في [BENCHMARKS.md](BENCHMARKS.md).

## النتائج

افتراض النموذج: 30 يومًا، 4 أنوية، 0.50 TU لكل ساعة نواة، تخزين 100 GiB بسعر افتراضي 0.20 TU/GiB شهريًا، تشغيل عمل ساعتين يوميًا، وتهيئة 0.25 ساعة وأعباء شهرية 30 TU للتشغيل المجدول. TU وحدات تعليمية وليست ريالًا ولا سعر مزود.

بلغ التشغيل الدائم 1460 TU، والمجدول 185 TU، والفرق 1275 TU. التعادل عند 23.25 ساعة عمل يوميًا؛ عند 23.75 ساعة يصبح المجدول 1490 TU، أي أعلى بـ30 TU. نجحت الفحوص الخمسة: المجاميع والتعادل والمثال المضاد والسعر الصفري ورفض المدخل السالب.

في القياس الفعلي تطابقت 72 رحلة و72 أجرة غير فارغة وإجمالي 1794.60 ريالًا في البديلين. وسيط CSV هو 0.263054 ثانية، ووسيط Delta version 0 هو 2.042495 ثانية. هذه أزمنة هذه العينة وبيئتها فقط.

## إجابات أسئلة اليوم الأول: 4 و5

**4. لماذا ليس الإيقاف أرخص دائمًا؟** تضاف كلفة بدء التشغيل والأعباء الثابتة إلى ساعات العمل؛ تتضاءل وفورات الإيقاف كلما اقترب العمل من 24 ساعة يوميًا. المثال المضاد عند 23.75 ساعة يرفض الاستنتاج العام. النتيجة حساسة لافتراضات النموذج ولا تعادل فاتورة سحابية.

**5. لماذا مقارنة 72 بــ144 غير عادلة؟** تختلف كمية العمل ومجموعة الصفوف؛ وقد تشمل ساعة القياس إنشاء الجلسة أو الاستيعاب في بديل دون الآخر؛ وتتأثر القراءات المتكررة بذاكرة النظام وJVM. كذلك لا تمثل العينة الصغيرة كل أنواع الأحمال. المقارنة المنفذة استخدمت Delta version 0، وتهيئة واحدة و4 تكرارات لكل بديل مع تبديل ترتيب التنفيذ، وتحققت من تطابق النتيجة وبصمة البيانات. الاستنتاج المحدود: CSV كان أقل زمنًا في هذه التجربة الصغيرة، مع بقاء مزايا سجل معاملات Delta موضوعًا مختلفًا عن سرعة هذه القراءة.

## الإعادة والقرار والحدود

الأزمنة الأربعة قياسات متكررة فعلية؛ ليست أربع جولات مستقلة ببيئات باردة. لم تُضبط ذاكرة OS/JVM/Delta بالكامل ولم تسجل مواصفات CPU/RAM التفصيلية. لا نعلن تسارعًا مضمونًا أو تكلفة إنتاجية. تقرير `reports/benchmark.json` وخطتا الاستعلام محفوظة في أرشيف اليوم الخامس. نتائج التكلفة موجودة في الدفتر، لكن ملف `cost_model_result.json` المنفصل يحتاج الاسترجاع من أرشيف اليوم الأول.


## تحديث التقارير المستقلة

أُعيد تنفيذ فحص المصدر ونموذج التكلفة فعليًا ضمن [التشغيل الكامل الناجح](https://github.com/amalkhotani/masar-modern-data-engineering/actions/runs/35267123057). يوجد `outputs/source_inspection.json` و`outputs/cost_model_result.json` داخل [أرشيف المخرجات](https://github.com/amalkhotani/masar-modern-data-engineering/releases/tag/verification-35267123057). هذه ملفات المحاولة الجديدة وليست استرجاعًا لملفي جلسة Colab المفقودين. تبقى قياسات الأداء التاريخية أعلاه مرتبطة ببيئتها الأصلية.
