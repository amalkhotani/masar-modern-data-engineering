# LAB07 — بناء Gold والتعافي من فشل نسخة مرشحة

## التعريف

- المتدربة: أمل خوتاني — Amal Khotani.
- البرنامج: هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy
- مادة الدورة والتنفيذ المشترك: ميعاد المري — Meaad Al-Marri. استُخدمت دوال المقرر مع تهيئة البيئة في Colab.
- تاريخ تشغيل Gold المثبت في سجلات Delta: 16 سبتمبر 2026؛ وقت قطع بيانات AI داخل التمرين هو 4 يونيو 2026، وهو تاريخ سيناريو مستقل عن تاريخ التشغيل.
- البيئة الموثقة في الدفتر: Google Colab CPU، Python 3.11.13، Java 17.0.20، Spark 3.5.8، Delta Spark 3.3.3، Py4J 0.10.9.9.
- البيانات: `MASAR_SMALL_V1`، بيانات اصطناعية. بصمة سجلها: `20a7e45bed2980b9394c10e8532da3b9f40f614366bb2df26a88610253e768e3`.
- مساحة العمل التراكمية نسبةً إلى جذر المستودع: `outputs/day01_bronze_xgnqdyt3`.
- دفتر التنفيذ المرفوع: `amal_khotani (3).ipynb`. استخدم أرقام التنفيذ الموضحة أدناه؛ الدفتر يجمع جلسات متعددة، فلا تكفي أرقام التنفيذ وحدها لترتيب كامل تاريخه.
- مصدر مراجعة النتائج: الدفتر المنفذ و`day05_handoff.zip`. جرى فحص التقارير وبصمات الملفات وقراءة صادرات CSV؛ لم يُعد تشغيل Spark أثناء إعداد هذه الملاحظات.
- نسخة التعليمات المرجعية: `8d98b9275296ae063e04b1d9088573de4ff6ca7d`. استُنسخ فرع `main` في جلسة الاستعادة، لكن معرّف Git الفعلي لتلك النسخة لم يُطبع في الدفتر؛ لا يُنسب معرّف التعليمات تلقائيًا إلى تشغيل المتدربة.

## ما نُفّذ

استُعيدت مساحة اليوم الرابع من الأرشيف، وتحقق الدفتر من تقريري التدفق والجودة. بعد تفعيل Java 17 نُفذت خلية اللاب ذات رقم التنفيذ `[12]`، باستدعاء `run_recovery_exercise(spark, SOURCE, WORK)` ثم `validate_stage_result("lab07_gold_recovery", result)`. ظهر `LAB07_PASSED` ونجحت الفحوص الأربعة. يستخدم هذا اللاب `kafka=False` لأنه يقرأ جداول Delta المحفوظة.

المدخلان المثبتان في سجل الإصدار هما جدول الرحلات المعتمد، وعدده **75** ونسخته **0**، وجدول أحداث GPS المختلفة، وعدده **217** ونسخته **0**. تحقق سجل الإصدار أيضًا من بصمتي تقريري الجودة والتدفق السابقين.

## الأدلة ونتيجة التعافي

المسارات التالية نسبةً إلى `WORK` داخل أرشيف اليوم الخامس:

| الدليل | المسار أو المعرف الفعلي |
|---|---|
| تقرير التعافي | `reports/day05_recovery.json` |
| مؤشر الإصدار المختار | `reports/day05_gold_latest.json` |
| سجل الإصدار المختار | `mini_lakehouse/serving_releases/147d2aa43f48468abed9551ef993a263/release.json` |
| الإصدار السليم الأول | `a01772a239974806ac420abc6f57f7bb` |
| النسخة المرشحة الفاشلة | `b81fca43b81d4d3d87b8d01d6859b67a` |
| تقرير الفشل المقصود | `reports/day05_failures/b81fca43b81d4d3d87b8d01d6859b67a.json` |
| الإصدار السليم بعد إعادة البناء | `147d2aa43f48468abed9551ef993a263` |

| الفحص المسجل | النتيجة | دلالته |
|---|---|---|
| `injected_failure_observed` | `true` | حدث الفشل المقصود بعد كتابة أول جدول. |
| `previous_release_preserved` | `true` | بقي الإصدار السليم السابق مختارًا بعد الفشل. |
| `rebuild_has_new_identity` | `true` | حمل البناء السليم اللاحق معرفًا جديدًا. |
| `content_equal` | `true` | تطابقت بصمات محتوى الجداول الثمانية في البنائين السليمين. |

## ١. لماذا يختلف عدد صفوف الوقائع عن ملخص السائقين؟

يمثل الصف في `bi.fact_trips` **رحلة واحدة فريدة** بمفتاح `trip_id`؛ لذلك يحتوي 75 صفًا. ويمثل الصف في `gold.driver_daily` **سائقًا وتاريخ بداية رحلة محليًا** بمفتاح `driver_key + trip_date_local`؛ لذلك يضم 18 مجموعة، وهي ستة سائقين عبر ثلاثة أيام في هذه العينة. لا تعني المجموعات الـ18 وجود 18 رحلة.

تثبت قراءة الصادرات الفعلية أن مجموع `trip_count` في ملخص السائقين هو **75**، ومجموع الأجور **1,880.60 ريال**، ومجموع المدد **95,400 ثانية**، وكلها تطابق الوقائع. كذلك يطابق الملخص الساعي هذه المجاميع. احتواؤه على 75 مجموعة خاص بالعينة، وليس شرطًا عامًا لتساوي عدد مجموعات الساعة وعدد الرحلات.

يصف `zone_key` مدينة تمثيلية، وتسجل الأبعاد `zone_resolution=city_proxy`. لا يثبت ذلك حدود أحياء أو مناطق خدمة جغرافية معتمدة. وتُفسّر مقاييس «الطلب» هنا كبدايات رحلات مرصودة، لا كقياس لكل طلبات السوق.

## ٢. كيف حُمي إجمالي الأجور عند الربط بأحداث GPS؟

قد تحتوي الرحلة على عدة أحداث GPS. ربط كل حدث مباشرةً بصف الرحلة ثم جمع الأجور يكرر أجرة الرحلة بعدد أحداثها. لذلك تُجمع الأحداث المختلفة حسب الرحلة أولًا، ثم يُربط العدد بالوقائع، مع إبقاء الرحلة صفًا واحدًا.

تُحفظ أيضًا الرحلات التي لا تملك أحداثًا مسجلة. في ملف الوقائع، الرحلات `SYN_LATE001` و`SYN_LATE002` و`SYN_LATE003` باقية وعدد أحداث كل منها صفر، بدل إسقاطها بربط داخلي. يصل مجموع `gps_event_count` إلى **217**، مع بقاء الأجور **1,880.60 ريال**.

يسجل لاب التدفق **219 رسالة مستقبلة** في المرحلة الأخيرة، مقابل **217 معرف حدث مختلفًا**. عدد الرسائل يقيس النقل بما فيه التسليم المكرر، بينما العدد المختلف يعكس الأحداث بعد إزالة التكرار باستخدام معرف الحدث. لا يُستخدم عدد الرسائل بوصفه عدد أحداث أعمال فريدة.

## ٣. أي إصدار بقي متاحًا بعد الفشل المقصود؟

بقي الإصدار الأول `a01772a239974806ac420abc6f57f7bb` مختارًا خلال فشل المرشح `b81fca43b81d4d3d87b8d01d6859b67a`. سجل الفشل يحدد `InjectedFailure` ورسالة `Deliberate failure after table 1`، والجدول المكتوب هو `gold.zone_hourly_demand` فقط. كذلك يسجل `previous_pointer_preserved=true` و`committed_release_exists=false`.

وجود ملفات جدول واحد لا يجعل المرشح إصدارًا مكتملًا: لا يوجد له `release.json` مكتمل، ولم ينتقل إليه مؤشر الإصدار. لذلك يقرأ المستهلك سجل الإصدار الذي يحدده المؤشر، ولا يجمع ملفات مجلدات الإصدارات المختلفة. بعد إعادة البناء الناجحة أصبح الإصدار `147d2aa43f48468abed9551ef993a263` هو المختار النهائي. احتُفظ بتقرير الفشل وملفاته للتشخيص.

## ٤. ماذا تثبت إعادة البناء، وما حدودها؟

يوثق المعرف الجديد أن البناء اللاحق محاولة مستقلة، بينما تثبت بصمات المحتوى المتساوية تطابق بيانات الأعمال في الجداول الثمانية. لا يلزم أن تتساوى أسماء ملفات Parquet أو بصمات بايتاتها بين المحاولتين، لأن هوية الكتابة وبياناتها الوصفية قد تختلف.

تثبت التجربة في جلسة Spark واحدة أن فشلًا مقصودًا لا يبدل مؤشر الإصدار السليم، وأن الإصدار السابق يظل مقروءًا، وأن إعادة البناء تنتج المحتوى نفسه بمعرف جديد. لا تثبت هذه التجربة معاملة ذرية واحدة للجداول الثمانية، أو أمان ناشرين متزامنين، أو التعافي من تعطل الجهاز. يسجل كل جدول Delta معاملته مستقلًا؛ والمؤشر المحلي يحدد الإصدار الكامل الذي يقرؤه المستهلك المرفق.

## القرار والخطوة التالية

اعتماد الإصدار السليم الجديد بعد اكتمال الفحوص، وتشغيل لاب 8 عليه. لا يُنقل المؤشر يدويًا إلى المرشح الفاشل. إذا أُعيد لاب 7 مستقبلًا، يُعاد لاب 8 بعده ثم يعاد إنشاء ZIP حتى تتوافق الصادرات مع الإصدار المختار.

ملاحظة مرصودة: نجح التعافي مع بقاء ملفات المحاولة الفاشلة؛ معيار اختيار الإصدار هو اكتمال سجل الإصدار وتحققه، وليس مجرد وجود ملفات على القرص.

## المراجع

- [قالب ملاحظات اللاب](https://github.com/amalkhotani/masar-modern-data-engineering/blob/8d98b9275296ae063e04b1d9088573de4ff6ca7d/templates/LAB_NOTES.md).
- [شرح لاب 7](https://github.com/amalkhotani/masar-modern-data-engineering/blob/8d98b9275296ae063e04b1d9088573de4ff6ca7d/day05/labs/lab07/WALKTHROUGH.md).
- [تعريفات الجداول](https://github.com/amalkhotani/masar-modern-data-engineering/blob/8d98b9275296ae063e04b1d9088573de4ff6ca7d/day05/DATA_PRODUCTS.md).
- [الأسئلة 1–4](https://github.com/amalkhotani/masar-modern-data-engineering/blob/8d98b9275296ae063e04b1d9088573de4ff6ca7d/day05/PRACTICE.md).
- الأدلة الخاصة بالتشغيل: الدفتر المرفوع والمسارات الفعلية داخل `day05_handoff.zip` المبينة أعلاه.
