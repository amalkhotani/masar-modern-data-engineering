# LAB06 — الجودة والعزل واعتماد لقطة نظيفة

أمل خوتاني — Amal Khotani. برنامج **هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)**، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy

مواد الدورة ودوالها: **ميعاد المري — Meaad Al-Marri**. هذه ملاحظات مبنية على المخرجات المحفوظة في الدفتر المرفوع والتقارير الأصلية؛ لم يُعد تشغيل Spark أثناء إعدادها. تفاصيل البيئة وبصمات الأدلة وحدود التحقق في [EVIDENCE_INDEX.md](EVIDENCE_INDEX.md).

## التعريف وما نُفذ

اليوم الرابع في [day04/STUDENT.ipynb](day04/STUDENT.ipynb)، الخلية الأصلية 74. استُخدمت Great Expectations 1.7.0 وpandas 2.2.3 بعد معالجة غياب الأولى وعدم تطابق إصدار الثانية، وشُغّل `scripts/run_day04.py --part quality` في عملية جديدة. معرف التشغيل `73132b9541884ec0a7f1e10a014fa712`. ظهر `LAB06_PASSED` ونجحت الفحوص التسعة.

## النتائج الفعلية

| الحالة | الصفوف | GX success |
|---|---|---|
| trusted | 75 | true |
| mixed | 82 | false |
| rechecked | 75 | true |

المرشح المختلط: 82 = 75 مقبولة بالقواعد + 7 معزولة، ونسبة الرفض 8.54%. فشل المرشح مقصود ولم يُرقَّ. أُعيد فحص مجموعة الـ75 بصورة مستقلة قبل الاعتماد، وبقي إجمالي الأجور 1880.60 ريالًا ومحتوى Silver الأصلية دون تغيير.

| رمز العيب | الصفوف |
|---|---|
| `INVALID_CITY` | 1 |
| `INVALID_DISTANCE` | 1 |
| `INVALID_DURATION` | 1 |
| `INVALID_FARE` | 1 |
| `INVALID_TIMESTAMP` | 1 |
| `MISSING_TRIP_ID` | 1 |
| `UNKNOWN_DRIVER` | 1 |

التقرير `reports/day04_quality_latest.json`، وسياسة المرشح `reports/day04_quality/73132b9541884ec0a7f1e10a014fa712/mixed_policy.json`، واللقطة المعتمدة `mini_lakehouse/silver/validated_day04/73132b9541884ec0a7f1e10a014fa712`، والعزل `mini_lakehouse/quarantine/day04/73132b9541884ec0a7f1e10a014fa712`. نتائج GX وData Docs محفوظة تحت مسار التقرير لكل من trusted وmixed وrechecked. تقرير `mixed_policy.json` يذكر `gx_executed=false` لأنه تقييم سياسة حسابي؛ دليل GX الفعلي مستقل في `gx_results` وملفات Checkpoint.

## إجابات أسئلة اليوم الرابع: 5–8

5. **60 صفًا صالحًا لا تكفي:** فحص `ExpectTableRowCountToEqual(value=75)` وسياسة حجم الدفعة يمنعان اعتماد مجموعة ناقصة. نسبة رفض صفرية لا تكشف صفوفًا لم تصل أصلًا؛ يلزم التحقق من الاكتمال كذلك.
6. **لماذا يفشل مرشح 82؟** الحجم لا يطابق 75 وتوجد مخالفات صفية. مثال جذري: `SYN_BAD002` بأجرة `-5.00`، والسبب `INVALID_FARE`. تحفظ العزلة الصف وأسبابه وإصدار القاعدة `MASAR_QUALITY_V1`. لا تكفي إزالة السبعة ثم استخدام الباقي فورًا؛ يجب نجاح السياسة وGX في فحص جديد للـ75 ثم مطابقة محتواها مع Silver قبل النشر.
7. **الساعة وخط المقارنة:** ساعة السيناريو `2026-06-04T03:04:00Z`. عمر الوصول 240 ثانية ضمن حد 300، وعمر أحدث حدث 33540 ثانية ضمن حد 43200؛ كلاهما PASS. هذه فروق محسوبة إلى ساعة ثابتة، وليست قياسات حية لكمون الشبكة. مسافة توزيع المدن هي total variation distance = 0.0 مقابل خط أساس 25 رحلة لكل مدينة، والحد 0.1. ذلك يثبت تطابق هذا التوزيع مع baseline التعليمي، لا غياب الانحراف في مجتمع حقيقي ولا دلالة إحصائية عامة.
8. **حدود النقل والحوكمة:** ينقل الأرشيف اللقطات والأدلة ولا يعيد تخزين Kafka خارج WORK. وجود مجلد `approved` أو تقرير GX ناجح لا يفرض RBAC أو TLS أو امتثالًا نظاميًا. تراجع [GOVERNANCE.md](GOVERNANCE.md) الضوابط المنفذة والمقترحة.

## القرار والحدود

يستخدم اليوم الخامس حقل approved_table من تقرير الجودة وحقل event_table من تقرير التدفق، لا المرشح المختلط ولا جدول العزل. فحص GX يجمع لقطة صغيرة إلى pandas بحد تعليمي أقصى 500 صف في الدالة؛ ليس تنفيذ جودة موزعًا لاختبار أحجام كبيرة. نجاح التنفيذ لا يمثل شهادة امتثال.
