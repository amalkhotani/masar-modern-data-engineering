# BENCHMARKS — القياسات المرصودة وحدودها

أمل خوتاني — Amal Khotani. برنامج **هندسة البيانات الحديثة لأنظمة الذكاء الاصطناعي — Modern Data Engineering for AI Systems (SDA-DSC-214)**، [أكاديمية سدايا](https://github.com/SDAIAAcademy). #SDAIAAcademy

مواد الدورة ودوالها: **ميعاد المري — Meaad Al-Marri**. هذه ملاحظات مبنية على المخرجات المحفوظة في الدفتر المرفوع والتقارير الأصلية؛ لم يُعد تشغيل Spark أثناء إعدادها. تفاصيل البيئة وبصمات الأدلة وحدود التحقق في [EVIDENCE_INDEX.md](EVIDENCE_INDEX.md).

## عقد قياس اليوم الأول

- المدخل: 72 رحلة أساسية لكل بديل؛ CSV مقابل Delta version 0 قبل إعادة التسليم. تطابقت بصمة المحتوى `7257378390b1239dd31bb94fb9f8277f0bba15044b797f639cd31a094dd966ba`.
- البيئة الموثقة: Google Colab CPU، Python 3.11.13، Java 17.0.20، Spark 3.5.8، Delta Spark 3.3.3. لم تُسجل طرازات المعالج وحجم RAM والتخزين تفصيليًا؛ لا تُفترض مواصفات عتاد.
- الاستعلام: حساب عدد الصفوف وعدد الأجور غير الفارغة ومجموع fare_sar بعد التحويل المناسب. النتيجة 72 و72 و1794.60 ريالًا في البديلين.
- التهيئة: قراءة warm-up واحدة لكل بديل، ثم 4 قياسات لكل بديل بترتيب متناوب CSV/Delta ثم Delta/CSV.
- نطاق الزمن: بناء الاستعلام والتخطيط وتنفيذ Spark وcollect؛ يستثني بدء الجلسة والاستيعاب.
- التخزين المؤقت: لا يوجد cache صريح لـDataFrame؛ ذاكرة OS/JVM وبيانات Delta الوصفية غير مضبوطة بالكامل.

| البديل | العينة 1 (ث) | العينة 2 | العينة 3 | العينة 4 | الوسيط |
|---|---|---|---|---|---|
| csv | 0.212241 | 0.263055 | 0.263052 | 0.419637 | 0.263054 |
| delta_v0 | 1.570837 | 1.706013 | 2.491080 | 2.378976 | 2.042495 |

الدليل الأصلي `reports/benchmark.json`؛ الخطط `reports/plans/csv.txt` و`reports/plans/delta_v0.txt` داخل WORK. الفحوص الأربعة true. بقيت عينة CSV أسرع في هذه الجولة؛ لا يثبت ذلك ترتيبًا عامًا أو سعرًا سحابيًا أو قدرة توسع. حدود min/max: CSV من 0.212241 إلى 0.419637 ثانية، وDelta من 1.570837 إلى 2.491080 ثانية.

## قياس صيانة اليوم الثالث

| المؤشر | قبل OPTIMIZE | بعد OPTIMIZE |
|---|---:|---:|
| عدد الملفات النشطة | 4 | 1 |
| بايتات الملفات النشطة | 28486 | 12739 |

التقرير `reports/day03_maintenance_latest.json`، run_id `49d000d16d594d5aab996182e96956bd`. عملية الصيانة على نسخة منفصلة، وثبت `compaction_preserves_values=true`. سجلت المقاييس إضافة ملف واحد وإزالة أربعة من اللقطة النشطة. لا تعني الإزالة من اللقطة حذف ملفاتها التاريخية؛ كانت معاينة VACUUM فقط ولم تحذف ملفات. `speedup_claimed=false`؛ لم تُقَس هنا مقارنة زمن قراءة قبل/بعد compaction، لذلك لا ندّعي تسارعًا.

## التكلفة الافتراضية، منفصلة عن الأداء

نموذج [LAB02_NOTES.md](LAB02_NOTES.md): التشغيل الدائم 1460 TU والمجدول 185 TU عند ساعتين عمل يوميًا. نقطة التعادل 23.25 ساعة، والمجدول 1490 TU عند 23.75 ساعة. TU وحدة حسابية تعليمية وليست عملة. لا تُحوَّل الثواني المحلية إلى تكلفة مزود بلا قياس وبيانات تسعير مستقلة.

## ما يحسّن قوة القياس لاحقًا

لتعميم النتيجة يلزم توثيق العتاد والحجم بالبايت، وتكبير مجموعات ممثلة للأحمال، وفصل حالات warm/cold، وتكرارات كافية مع بيان التشتت، وقياس الاستعلام المطلوب فعليًا. هذه إجراءات مقترحة ولم تُنفذ ضمن النتائج أعلاه.


## تحديث التقارير المستقلة

أُعيد تنفيذ فحص المصدر ونموذج التكلفة فعليًا ضمن [التشغيل الكامل الناجح](https://github.com/amalkhotani/masar-modern-data-engineering/actions/runs/35267123057). يوجد `outputs/source_inspection.json` و`outputs/cost_model_result.json` داخل [أرشيف المخرجات](https://github.com/amalkhotani/masar-modern-data-engineering/releases/tag/verification-35267123057). هذه ملفات المحاولة الجديدة وليست استرجاعًا لملفي جلسة Colab المفقودين. تبقى قياسات الأداء التاريخية أعلاه مرتبطة ببيئتها الأصلية.
